# Подготовка переноса в production — 2026-09-24

Статус: подготовлен состав переноса. Production не изменён. Процедура: TEST_TO_PROD_TRANSFER.md.

## Подтверждено

Пользователь проверил в Workbench серые сферы, тексты ошибок, раздельное расположение сообщений, пересечения со статическими объектами, стрелку с высотой от рельефа и подписью deg. Подтверждены исправления посторонних полигонов и исчезновения сферы при изменении ракурса.

Snapshot схемы 2: script.log сессии logs_2026-09-23_20-27-26, 20:47:12.353 — status=SUCCESS scopes=4 labels=80 prefabLabelLinks=941. Текущий конфиг имеет schema=2 / generator=ME_labels_generator_v3. Это подтверждение генерации и round-trip, а не всех сценариев ambient spawning.

## Состав переноса

Основной файл: Scripts/Game/Components/Locations/ME_DebugAmbientVehicleSpawnPointComponent.c в каждом аддоне. Переносить блоками: файлы Test и Prod заметно различаются.

1. Предупреждения фильтра: серый цвет при конфликте include/exclude, отсутствии кандидатов или недоступности каталога; ERROR для конфликта и отсутствия кандидатов; WARNING для недоступного каталога; раздельные строки и include/exclude. Сохранить корректное пояснение requireAll=false. Runtime-фильтрацию не заменять.
2. Текстовые сообщения о пересечении сфер точек и границ статических объектов с именами/координатами; очистка и обновление всех текстовых объектов. Использовать существующие геометрические проверки.
3. Стрелка направления: локальная +Z, тот же yaw, длина 2,7 м и ширина наконечника 1,5 м; высота 0,3 м над максимальной выборкой рельефа. Подпись угла 0–359 в формате «90 deg» над стрелкой; обновление и очистка вместе с preview.
4. Исправления отрисовки: CreateTris — 12 треугольников для 36 вершин габаритов и 3 для 9 вершин стрелки. В production пока остаётся ошибочный вызов fillPoints, 36. NOZWRITE — для прозрачной заливки габаритов и сфер; тест глубины сохранить.

Вспомогательные методы выбора каталога уже существуют в production. Проверить зависимости новых полей и методов, сохранить super-вызовы. Комментарии production — на английском. Не переносить целиком test-only overrides, отладочные Print/PrintFormat и закомментированные эксперименты. Текущие пользовательские тексты ошибок входят в перенос; расширенные диагностические логи остаются в Test.

## Что остаётся в Test

Генератор и конфиг editable entity labels, Workbench diagnostic plugins, runtime instrumentation, autotest-миры, VBT-зависимость. resourceDatabase.rdb не копировать между аддонами. Новые возможности визуализации не требуют snapshot меток в production.

## Финальная проверка Test

- Обычная точка: зелёная сфера на свободном месте, корректные габариты, стрелка и подпись при вращении/перемещении.
- Конфликт меток и APC_ARMED: серая сфера, отдельные сообщения; после исправления фильтра предупреждения исчезают.
- Пересечение двух точек и ConcretePanel: сообщения появляются и исчезают после устранения пересечения.
- Удаление точки: нет оставшихся Shape/текстов; обновляются предупреждения соседних точек.
- Разные ракурсы: нет посторонних полигонов и исчезновения сферы внутри габаритов; непрозрачные объекты корректно закрывают подсказки.
- После перезагрузки: нет новых ошибок компиляции/исключений; ожидаемые диагностические ERROR проблемных fixture-точек не считать ошибкой скрипта.

После переноса повторить эти проверки в production отдельно. Обновить документы, перечисленные в TEST_TO_PROD_TRANSFER.md; changelog — под фактический выпуск. Сейчас коммит/публикация/перенос автоматически не выполняются.